Route Versioning -> 
* Think there are 2 versions of products api -> {/api/v1/products} ; {/api/v2/products}.
* If we take one example you must have visited restrurant late night when they are closing it they do not kick all the people those who are eating there meal , So for the solution of this problem we don’t allow new customers to enter and let the existing ones to finish up and leave.
* Same happens in the route versioning in this the v1 run for some time so that users can migrate to the v2 the latest version and after sometime they can close it when most of the users have mingrated to the new version.
* So by this we can solve the problem of deprecating v1 at the same time as the v2 have launched we can give some time to the uses to shift to the v2 and the eventually close it.

