Static Routes ->
* Lets take an example :- GET api/books
* This is a route in which no parameters changes obviously it will change if you will ask for some different resource but in this path remains fixed and doesn’t change.
* So it is fixed and don’t have any variable parameter.
